<Window x:Class="SlowWhy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SlowWhy - Sistem Analizi" Height="450" Width="800"
        Background="#1E1E1E">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Sistem Sağlık Durumu" 
                   Foreground="White" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#2D2D30" CornerRadius="10" Margin="10">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="İŞLEMCİ (CPU)" Foreground="#AAAAAA" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="txtCpu" Text="%0" Foreground="#FF5C5C" FontSize="40" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="Kullanım" Foreground="#AAAAAA" HorizontalAlignment="Center" FontSize="10"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#2D2D30" CornerRadius="10" Margin="10">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="BOŞ RAM" Foreground="#AAAAAA" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="txtRam" Text="0 MB" Foreground="#4CD964" FontSize="40" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="Kullanılabilir Alan" Foreground="#AAAAAA" HorizontalAlignment="Center" FontSize="10"/>
                </StackPanel>
            </Border>
        </Grid>

        <Button x:Name="btnAnaliz" 
                Grid.Row="2"
                Content="Analizi Başlat" 
                Height="50" 
                Width="200"
                Background="#007ACC"
                Foreground="White"
                FontSize="16"
                FontWeight="Bold"
                Click="btnAnaliz_Click"
                Margin="0,20,0,0">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="25"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>
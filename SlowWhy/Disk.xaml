<UserControl x:Class="SlowWhy.Disk"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="400">

    <Grid Background="#F0F0F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#D0D0D0" BorderThickness="0,0,0,1" Padding="10">
            <Grid>
                <Button x:Name="btnBack" Content="← Back" HorizontalAlignment="Left" Width="50" Height="25" Background="Transparent" BorderThickness="0" Foreground="#0078D7" Click="btnBack_Click" Cursor="Hand"/>
                <TextBlock Text="Disk Analysis (App &amp; Files)" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#333" FontSize="11"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="10">
            <DataGrid x:Name="dgItems" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                      Background="White" 
                      Foreground="#333"
                      BorderBrush="#ADADAD"
                      BorderThickness="1"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#E0E0E0"
                      HeadersVisibility="Column"
                      RowHeight="28"
                      FontSize="11"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto" 
                      ScrollViewer.VerticalScrollBarVisibility="Auto">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="180"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="80" FontWeight="Bold"/>
                    <DataGridTextColumn Header="Size" Binding="{Binding DisplaySize}" Width="80"/>
                    <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="300"/>
                </DataGrid.Columns>

                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="📂 Open Location" Click="MenuItem_OpenLocation_Click"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
            </DataGrid>

            <StackPanel x:Name="pnlLoading" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                <TextBlock Text="Scanning..." Foreground="#0078D7" FontWeight="Bold" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Background="White" BorderBrush="#D0D0D0" BorderThickness="0,1,0,0" Padding="10">
            <Button x:Name="btnScan" 
                    Content="Scan" 
                    Height="35" 
                    Background="#0078D7" 
                    Foreground="White" 
                    BorderThickness="0" 
                    FontWeight="SemiBold"
                    Cursor="Hand"
                    Click="btnScan_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="2"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Border>

        <StatusBar Grid.Row="3" Name="sbar" VerticalAlignment="Bottom" Background="#EFEFEF" BorderBrush="#CCC" BorderThickness="0,1,0,0">
            <StatusBarItem>
                <TextBlock x:Name="txtStatus" Text="Ready" FontSize="11" Foreground="#444"/>
            </StatusBarItem>

            <Separator/>

            <StatusBarItem>
                <ProgressBar x:Name="progressBar1" Width="120" Height="15" Minimum="0" Maximum="100" />
            </StatusBarItem>
        </StatusBar>

    </Grid>
</UserControl>
<UserControl
  x:Class="SlowWhy.GPU"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:hc="https://handyorg.github.io/handycontrol"
  xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  d:DesignHeight="500" d:DesignWidth="450">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Padding="12,10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
      <Grid>
        <Button
          HorizontalAlignment="Left"
          Click="btnBack_Click"
          Style="{StaticResource ButtonDefault}"
          Padding="8,4">
          <StackPanel Orientation="Horizontal">
            <icon:PackIconPhosphorIcons Kind="ArrowLeft" Width="14" Height="14" VerticalAlignment="Center" />
            <TextBlock Margin="4,0,0,0" Text="Back" VerticalAlignment="Center" />
          </StackPanel>
        </Button>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
          <icon:PackIconPhosphorIcons Kind="Monitor" Width="16" Height="16" VerticalAlignment="Center" Foreground="{DynamicResource WarningBrush}" />
          <TextBlock Margin="6,0,0,0" Text="Graphics Card (GPU)" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center" />
        </StackPanel>
      </Grid>
    </Border>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
      <hc:UniformSpacingPanel Orientation="Vertical" Spacing="12" Margin="15">

        <Border
          Padding="20"
          Background="{DynamicResource RegionBrush}"
          BorderBrush="{DynamicResource BorderBrush}"
          BorderThickness="1"
          CornerRadius="4">
          <StackPanel>
            <TextBlock
              x:Name="txtGpuName"
              Text="Loading GPU Name..."
              FontWeight="Bold"
              FontSize="15"
              Margin="0,0,0,15"
              TextWrapping="Wrap" />

            <Grid Margin="0,0,0,5">
              <TextBlock Text="3D Usage" FontSize="13" VerticalAlignment="Bottom" />
              <TextBlock
                x:Name="txtUsage"
                Text="0%"
                FontSize="36"
                HorizontalAlignment="Right"
                Foreground="{DynamicResource PrimaryBrush}" />
            </Grid>

            <ProgressBar
              x:Name="pbUsage"
              Height="6"
              Maximum="100"
              Value="0" />
          </StackPanel>
        </Border>

        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>

          <Border Grid.Row="0" Grid.Column="0" Padding="15" Background="{DynamicResource RegionBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
            <StackPanel>
              <TextBlock Text="VRAM Memory" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" />
              <TextBlock x:Name="txtVramTotal" Text="-- GB" FontSize="18" FontWeight="SemiBold" Margin="0,5,0,0" />
            </StackPanel>
          </Border>

          <Border Grid.Row="0" Grid.Column="2" Padding="15" Background="{DynamicResource RegionBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
            <StackPanel>
              <TextBlock Text="Driver Version" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" />
              <TextBlock x:Name="txtDriver" Text="--" FontSize="12" FontWeight="SemiBold" Margin="0,5,0,0" TextWrapping="Wrap" />
            </StackPanel>
          </Border>

          <Border Grid.Row="2" Grid.Column="0" Padding="15" Background="{DynamicResource RegionBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
            <StackPanel>
              <TextBlock Text="Resolution" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" />
              <TextBlock x:Name="txtResolution" Text="-- x --" FontSize="14" FontWeight="SemiBold" Margin="0,5,0,0" TextWrapping="Wrap" />
            </StackPanel>
          </Border>

          <Border Grid.Row="2" Grid.Column="2" Padding="15" Background="{DynamicResource RegionBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
            <StackPanel>
              <TextBlock Text="Refresh Rate" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" />
              <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <TextBlock x:Name="txtRefresh" Text="--" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource SuccessBrush}" />
                <TextBlock Text="Hz" FontSize="12" VerticalAlignment="Bottom" Margin="3,0,0,4" Foreground="{DynamicResource SecondaryTextBrush}" />
              </StackPanel>
            </StackPanel>
          </Border>
        </Grid>

      </hc:UniformSpacingPanel>
    </ScrollViewer>
  </Grid>
</UserControl>

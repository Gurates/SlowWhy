<UserControl
  x:Class="SlowWhy.Ram"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:hc="https://handyorg.github.io/handycontrol"
  xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  d:DesignHeight="500" d:DesignWidth="400">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Padding="10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
      <Grid>
        <Button
          HorizontalAlignment="Left"
          Click="btnBack_Click"
          Style="{StaticResource ButtonIconCustom}"
          Padding="8,4">
          <StackPanel Orientation="Horizontal">
            <icon:PackIconPhosphorIcons Kind="ArrowLeft" Width="14" Height="14" VerticalAlignment="Center" />
            <TextBlock Margin="4,0,0,0" Text="Back" VerticalAlignment="Center" />
          </StackPanel>
        </Button>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
          <icon:PackIconPhosphorIcons Kind="Memory" Width="16" Height="16" VerticalAlignment="Center" Foreground="{DynamicResource SuccessBrush}" />
          <TextBlock Margin="6,0,0,0" Text="Memory (RAM) Optimizer" FontWeight="SemiBold" FontSize="11" VerticalAlignment="Center" />
        </StackPanel>
      </Grid>
    </Border>

    <Border
      Grid.Row="1"
      Margin="10,10,10,0"
      Padding="15"
      Background="{DynamicResource RegionBrush}"
      BorderBrush="{DynamicResource BorderBrush}"
      BorderThickness="1"
      CornerRadius="4">
      <Grid>
        <TextBlock Text="Available Memory" FontSize="12" VerticalAlignment="Bottom" />
        <TextBlock
          x:Name="txtRam"
          Text="0.00 GB"
          FontSize="32"
          HorizontalAlignment="Right"
          Foreground="{DynamicResource PrimaryBrush}" />
      </Grid>
    </Border>

    <Border
      Grid.Row="2"
      Margin="10,10,10,0"
      Padding="15"
      Background="{DynamicResource RegionBrush}"
      BorderBrush="{DynamicResource BorderBrush}"
      BorderThickness="1"
      CornerRadius="4">
      <StackPanel>
        <TextBlock Text="Cleaning Mode" FontSize="12" Margin="0,0,0,8" />
        <ComboBox x:Name="cmbCleaningLevel" SelectedIndex="0" Height="35" FontSize="12">
          <ComboBoxItem>
            <StackPanel>
              <TextBlock Text="Safe (Recommended)" FontWeight="SemiBold" Foreground="{DynamicResource SuccessBrush}" />
              <TextBlock Text="Cache only - No performance impact" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" />
            </StackPanel>
          </ComboBoxItem>
          <ComboBoxItem>
            <StackPanel>
              <TextBlock Text="Medium" FontWeight="SemiBold" Foreground="{DynamicResource WarningBrush}" />
              <TextBlock Text="Cache + File system - Minimal impact" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" />
            </StackPanel>
          </ComboBoxItem>
          <ComboBoxItem>
            <StackPanel>
              <TextBlock Text="Aggressive" FontWeight="SemiBold" Foreground="{DynamicResource DangerBrush}" />
              <TextBlock Text="Everything - May slow apps temporarily" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" />
            </StackPanel>
          </ComboBoxItem>
        </ComboBox>
      </StackPanel>
    </Border>

    <Border
      Grid.Row="3"
      Margin="10,10,10,0"
      Padding="15"
      Background="{DynamicResource RegionBrush}"
      BorderBrush="{DynamicResource BorderBrush}"
      BorderThickness="1"
      CornerRadius="4">
      <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel>
          <TextBlock Text="How it works:" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,10" />

          <TextBlock TextWrapping="Wrap" FontSize="11" Margin="0,0,0,5">
            <Run Text="Safe Mode:" FontWeight="Bold" />
            <Run Text=" Clears closed apps cache (1-2 GB)" />
          </TextBlock>

          <TextBlock TextWrapping="Wrap" FontSize="11" Margin="0,0,0,5">
            <Run Text="Medium Mode:" FontWeight="Bold" />
            <Run Text=" File system cache (1.5-2.5 GB)" />
          </TextBlock>

          <TextBlock TextWrapping="Wrap" FontSize="11" Margin="0,0,0,10">
            <Run Text="Aggressive Mode:" FontWeight="Bold" />
            <Run Text=" Running apps (2-4 GB, may slow)" />
          </TextBlock>

          <Border
            Padding="10"
            CornerRadius="4"
            Background="{DynamicResource SecondaryRegionBrush}"
            BorderBrush="{DynamicResource WarningBrush}"
            BorderThickness="1">
            <StackPanel Orientation="Horizontal">
              <icon:PackIconPhosphorIcons Kind="Lightbulb" Width="14" Height="14" VerticalAlignment="Top" Margin="0,2,8,0" Foreground="{DynamicResource WarningBrush}" />
              <TextBlock TextWrapping="Wrap" FontSize="10">
                <Run Text="Tip:" FontWeight="Bold" />
                <Run Text=" Use Safe mode for daily use. Only use Aggressive before launching games." />
              </TextBlock>
            </StackPanel>
          </Border>
        </StackPanel>
      </ScrollViewer>
    </Border>

    <Border Grid.Row="4" Padding="10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0">
      <Button
        x:Name="btnRamClear"
        Height="40"
        Click="btnRamClear_Click"
        Style="{StaticResource ButtonPrimary}">
        <StackPanel Orientation="Horizontal">
          <icon:PackIconPhosphorIcons Kind="Broom" Width="14" Height="14" VerticalAlignment="Center" />
          <TextBlock Margin="6,0,0,0" Text="Clean RAM" VerticalAlignment="Center" FontWeight="SemiBold" />
        </StackPanel>
      </Button>
    </Border>
  </Grid>
</UserControl>

<UserControl
  x:Class="SlowWhy.CPU"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:hc="https://handyorg.github.io/handycontrol"
  xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  d:DesignHeight="500"
  d:DesignWidth="400"
  mc:Ignorable="d">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Border
      Grid.Row="0"
      Padding="10"
      BorderBrush="{DynamicResource BorderBrush}"
      BorderThickness="0,0,0,1">
      <Grid>
        <Button
          Padding="8,4"
          HorizontalAlignment="Left"
          Click="btnBack_Click"
          Style="{StaticResource ButtonIconCustom}">
          <StackPanel Orientation="Horizontal">
            <icon:PackIconPhosphorIcons
              Width="14"
              Height="14"
              VerticalAlignment="Center"
              Kind="ArrowLeft" />
            <TextBlock
              Margin="4,0,0,0"
              VerticalAlignment="Center"
              Text="Back" />
          </StackPanel>
        </Button>

        <StackPanel
          HorizontalAlignment="Center"
          VerticalAlignment="Center"
          Orientation="Horizontal">
          <icon:PackIconPhosphorIcons
            Width="16"
            Height="16"
            VerticalAlignment="Center"
            Foreground="{DynamicResource PrimaryBrush}"
            Kind="Cpu" />
          <TextBlock
            Margin="6,0,0,0"
            VerticalAlignment="Center"
            FontWeight="SemiBold"
            Text="Processor (CPU)" />
        </StackPanel>
      </Grid>
    </Border>

    <Border
      Grid.Row="1"
      Margin="10"
      Padding="15"
      Background="{DynamicResource RegionBrush}"
      BorderBrush="{DynamicResource BorderBrush}"
      BorderThickness="1"
      CornerRadius="4">
      <StackPanel>
        <Grid>
          <TextBlock VerticalAlignment="Bottom" Text="Total Utilization" />
          <TextBlock
            x:Name="txtTotalUsage"
            HorizontalAlignment="Right"
            FontSize="36"
            Foreground="{DynamicResource PrimaryBrush}"
            Text="0%" />
        </Grid>
        <ProgressBar
          x:Name="pbTotalUsage"
          Margin="0,5,0,0"
          Maximum="100"
          Value="0" />
      </StackPanel>
    </Border>

    <Grid Grid.Row="2" Margin="10,0,10,10">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <TextBlock Margin="0,0,0,5" Text="Top Processes" />

      <DataGrid
        x:Name="dgProcesses"
        Grid.Row="1"
        AutoGenerateColumns="False"
        GridLinesVisibility="Vertical"
        HeadersVisibility="Column"
        IsReadOnly="True"
        RowStyle="{StaticResource CustomDataGridRowStyle}"
        Style="{StaticResource DataGrid.Small}"
        VerticalGridLinesBrush="{DynamicResource SecondaryRegionBrush}">
        <DataGrid.Columns>
          <DataGridTextColumn
            Width="2*"
            Binding="{Binding Name}"
            Header="Process Name" />
          <DataGridTextColumn
            Width="0.8*"
            Binding="{Binding Id}"
            Header="PID" />
          <DataGridTextColumn
            Width="1*"
            Binding="{Binding Usage}"
            Header="CPU %" />
        </DataGrid.Columns>
        <DataGrid.ContextMenu>
          <ContextMenu Style="{StaticResource ContextMenu.Small}">
            <MenuItem Click="CpuAppClose_Click" Header="Close Process">
              <MenuItem.Icon>
                <icon:PackIconPhosphorIcons
                  Width="14"
                  Height="14"
                  Kind="X" />
              </MenuItem.Icon>
            </MenuItem>
          </ContextMenu>
        </DataGrid.ContextMenu>
      </DataGrid>
    </Grid>
  </Grid>
</UserControl>
